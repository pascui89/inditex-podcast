import {
    CardContent,
    styled,
  } from '@mui/material';

export const StyledMainCardContent = styled(CardContent)`
    max-width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
`;